# Research: workflow — 2026-07-10

Query: scoping "workflow" before authoring a pattern entry — user vs agent workflow, builder vs display, orchestration altitude, the BPM boundary, and whether the strawman ("the actor externalises a procedure into an inspectable, editable representation that the system executes") survives contact with the literature. Generated via `/research`.

> *Retrieval provenance.* arxiv primary: 74 unique candidates across 6 query variants (trigger-action programming; end-user programming + automation; workflow+agent+human in cs.HC; visual programming + node; workflow authoring / automation rules / if-this-then-that; agent orchestration in cs.HC). Semantic Scholar rate-limited (HTTP 429) on all 4 queries, no API key — **lineage omitted**; run `/research refresh workflow` when S2 access returns. Rerank kept 8 of 74. Near-misses kept visible for context: *AgentSPEX* (2026, workflow-specification language between reactive prompting and code-coupled orchestration — formality-spectrum datum), *Graph of Trace* (ACL 2026 demo, execution-trace-as-graph display), *How Do AI Agents Do Human Work?* (2025, induces comparable workflow representations from human and agent activity — Q1 datum, labour-study transfer), *AwareAuto* (2024, LLM-mediated natural expression → complex automation rules), *OrchVis* (2025, hierarchical multi-agent oversight). The BPM/WfMS question drew almost nothing — see Promotion candidates.

## Context

Pattern-language entry scoping, delegation's neighbourhood. Three existing pages circle the concept without naming it: delegation's *tuning tools* touchpoint, bot's workflow-automation mode, ai-tuning's skeletal Workflow section. A tldraw node-canvas builder exists as substrate. The formality spectrum (natural language ↔ visual graph ↔ code) is settled vocabulary.

Questions:

1. Does "workflow" name one move or several — user workflow vs agent workflow; is the externalise-and-execute strawman conflating them?
2. Building vs reading — is a workflow *builder* the same pattern as a workflow *display* (diagram/canvas as monitoring or explanation surface)?
3. Where does orchestration sit — a distinct altitude above single-procedure automation?
4. What does end-user-programming / visual-programming literature say the representation costs — legibility vs expressiveness, and who authors (actor-built vs system-proposed-then-repaired)?
5. Where does BPM/workflow-management-systems territory begin, and does the project's scope end before it?
6. Is "workflow" the honest name for the move, or an artifact-noun hiding a better move name?

## Retrieved papers

- *ReUseIt: Synthesizing Reusable AI Agent Workflows for Web Automation* (IUI 2026) — synthesises durable workflows from a web agent's own successful and failed attempts; execution guards keep users informed; success 24.2%→70.1%. [arxiv:2510.14308](https://arxiv.org/abs/2510.14308)
  - Speaks to: Q1, Q2, Q4
  - Transfer: strong — the strawman's inverse in the wild: the *system* externalises the procedure; the actor inspects, reuses, repairs.
- *InstructPipe: Generating Visual Blocks Pipelines with Human Instructions and LLMs* (CHI 2025) — LLM drafts a pipeline from a text instruction and renders it in a node-graph editor for human refinement; targets the blank-workspace problem. [arxiv:2312.09672](https://arxiv.org/abs/2312.09672)
  - Speaks to: Q4
  - Transfer: strong — node canvas as *repair surface* rather than from-scratch authoring surface; directly applicable to the tldraw builder.
- *PromptChainer: Chaining Large Language Model Prompts through Visual Programming* (CHI LBW 2022) — LLM-chain authoring needs scaffolding for intermediate outputs and debugging at multiple granularities; chains perceived as more transparent and controllable than single runs. [arxiv:2203.06566](https://arxiv.org/abs/2203.06566)
  - Speaks to: Q2, Q4
  - Transfer: strong — the canonical builder-needs study; forces for the pattern's authoring side.
- *AIPOM: Agent-aware Interactive Planning for Multi-Agent Systems* (EMNLP 2025 demo) — human-in-the-loop planning over orchestrated multi-agent systems via paired conversational and graph interfaces; inspect, refine, guide. [arxiv:2509.24826](https://arxiv.org/abs/2509.24826)
  - Speaks to: Q2, Q3
  - Transfer: strong — at orchestration altitude the *plan* is the workflow artifact under the same inspect-and-repair contract.
- *XAgen: An Explainability Tool for Identifying and Correcting Failures in Multi-Agent Workflows* (2025) — formative interviews (12 practitioners) on opaque multi-agent failure; glanceable log visualisation, human feedback, LLM-as-judge error detection; users attribute failures to specific agents/steps. [arxiv:2512.17896](https://arxiv.org/abs/2512.17896)
  - Speaks to: Q2, Q3
  - Transfer: strong — the reading/repair face; maps onto delegation's exception-handling stage.
- *ModSandbox: Facilitating Online Community Moderation Through Error Prediction and Improvement of Automated Rules* (2022) — moderators' core difficulties are estimating a rule's effect in advance and knowing how to update it; a sandbox previews false positives/negatives against historical data. [arxiv:2210.09569](https://arxiv.org/abs/2210.09569)
  - Speaks to: Q2 (repair loop), Q5
  - Transfer: strong — pre-LLM grounding; effect-preview is delegation's *test drive* touchpoint with evidence behind it.
- *Situated Live Programming for Human-Robot Collaboration* (UIST 2021) — end users grow trigger-action programs incrementally, executing partial programs while refining them, authoring by annotating the live task space. [arxiv:2108.03592](https://arxiv.org/abs/2108.03592)
  - Speaks to: Q4
  - Transfer: partial (shop-floor robots) — but the grown-not-specified authoring model transfers cleanly to incremental formalisation.
- *Natural Notation for the Domestic Internet of Things* (IS-EUD 2015) — sticky-note reminders as end-user programming; the same instruction genre addressed to self, to others, or to a computer agent; informal graphical notation emerges spontaneously. [arxiv:1503.01895](https://arxiv.org/abs/1503.01895)
  - Speaks to: Q1, Q6
  - Transfer: strong — the executor-agnostic datum; instructing an agent is continuous with ordinary instruction-giving.

## Convergent findings

### C1. The workflow artifact is bidirectional — authored to delegate, read to supervise

ReUseIt's synthesised workflows "incorporate execution guards … while keeping users informed of progress and issues"; PromptChainer's chains are valued because they are "perceived to be more transparent and controllable" than an opaque single run; AIPOM and XAgen exist purely so users can "inspect, understand, and control" workflows that already run. The build surface and the monitoring surface converge on one representation. Building vs display (Q2) is not two patterns — it is one artifact's two faces, and systems increasingly ship both on the same graph view. Orchestration (Q3) does not break this: AIPOM and XAgen apply the identical inspect-refine contract to multi-agent plans, which reads as the same move at a higher composition, not a new move.

Papers: ReUseIt, PromptChainer, AIPOM, XAgen (near-miss corroboration: Graph of Trace, OrchVis).

*Draft project takeaway*: a workflow representation earns its keep twice — authored to delegate, read to supervise; the pattern should be one entry with two faces, not builder/display twins.

### C2. Authorship is inverting: system-proposed, actor-repaired

InstructPipe's premise is that "existing systems typically require users to build pipelines from scratch" and that the fix is an LLM drafting the pipeline "for further human-AI collaboration"; ReUseIt synthesises the workflow *from the agent's own attempts* with no authoring step at all; AwareAuto (near-miss) generates multi-branch automations from natural expression. The blank canvas is a dying assumption. This is the strongest correction to the strawman: "the actor externalises a procedure" is only half the pattern — increasingly the *system* externalises (from instructions, demonstrations, or its own runs) and the actor's move is *review and repair*. The externalisation itself is executor-agnostic; who drafts first is a dimension, not a definition.

Papers: InstructPipe, ReUseIt (near-miss: AwareAuto).

*Draft project takeaway*: name inspection-and-repair, not construction, as the pattern's core interaction — the draft may come from either side of the delegation boundary.

### C3. The repair loop is the hard part; it needs effect-preview and step-level attribution

ModSandbox's moderators could not "estimate the actual effect of a rule in advance" nor figure out "how the rules should be updated"; PromptChainer found chain authoring "requires … debugging the chain at multiple granularities"; XAgen's practitioners needed to "attribute [failures] to specific agents or steps". Authoring interfaces systematically underweight the after-deployment life of the procedure. The two load-bearing affordances are *rehearsal against real data before commitment* (ModSandbox's sandbox — evidence for delegation's *test drive* touchpoint) and *failure localisation to a step* (XAgen, PromptChainer).

Papers: ModSandbox, PromptChainer, XAgen.

*Draft project takeaway*: what makes automation hard is not writing the rule but foreseeing its effect and locating its failures — effect-preview and per-step attribution are the affordances the entry must name.

### C4. The instruction genre is executor-agnostic

Natural Notation's reminders "might be performed either by an automated agent or by the author" — the same sticky-note genre, addressed to self, another person, or a machine, with only linguistic register shifting. ReUseIt closes the loop from the other end: a workflow induced from *agent* behaviour is read and reused by *humans*. The near-miss occupations study (How Do AI Agents Do Human Work?) induces one workflow representation from either species of worker. Q1 resolves: user workflow vs agent workflow is not a pattern boundary; the executor is a dimension of one move. What varies by executor is trust apparatus (guards, monitoring), not the representational move.

Papers: Natural Notation, ReUseIt (near-miss: How Do AI Agents Do Human Work?).

*Draft project takeaway*: instructing a system is continuous with leaving a note for yourself or a colleague — one move, executor as a dimension; don't fork user-workflow and agent-workflow entries.

### C5. Workflows are grown, not specified

Situated Live Programming's users "iteratively create, edit, and refine a reactive robot program *while executing partial programs*", authoring in the live task space rather than an abstract canvas; Natural Notation's authors drift spontaneously into "graphical resources as an informal visual language"; AgentSPEX (near-miss) positions an explicit workflow language precisely between implicit reactive prompting and code-coupled orchestration frameworks. Formality is entered gradually and situationally — the entry should hook into the formality spectrum and incremental formalisation rather than assume upfront specification.

Papers: Situated Live Programming, Natural Notation (near-miss: AgentSPEX).

*Draft project takeaway*: the workflow representation is grown while partially running, sliding along the formality spectrum — link the entry to incremental formalisation and formality, and treat upfront full specification as the degenerate case.

## Lineage

Semantic Scholar was rate-limited throughout this run (no API key); 1-hop lineage requires S2 and is **omitted**. Expected convergent ancestors worth confirming on refresh: Ur et al.'s trigger-action programming studies (CHI 2014–), Huang & Cakmak on TAP mental models, Blackwell's attention-investment model of end-user programming, and the Wu et al. AI Chains paper (CHI 2022) that PromptChainer builds on. Run `/research refresh workflow` to fill this section.

## Promotion candidates

- [ ] *Strawman correction (C2)* — the pattern draft's "the actor externalises a procedure" must widen to cover system-drafted workflows the actor repairs; the core interaction is inspect-and-repair, drafting is bidirectional. This changes the entry's definition sentence.
- [ ] *One entry, not several (C1, C4)* — evidence against splitting builder/display or user-/agent-workflow; executor and authorship are dimensions. Feeds the naming question: "workflow" as artifact-noun survives (Form-like bilingual case) since the artifact is what all variants share.
- [ ] *ModSandbox → docs/references.md* — the effect-preview finding is direct evidence for delegation's *test drive* touchpoint; one-liner candidate.
- [ ] *PromptChainer (+ its AI Chains ancestor, pending lineage) → references/ distillation candidate* — chain/pipeline authoring forces (intermediate-output scaffolding, multi-granularity debugging) will recur across ai-tuning, prompt, and the workflow entry.
- [ ] *Natural Notation → docs/references.md* — the executor-agnostic instruction-genre finding underwrites the workflow↔delegation↔bot seam.
- [ ] *Retrieval gap, honest*: the BPM/WfMS boundary (Q5) drew nothing usable — that literature lives in BPM venues, not arxiv. Weak signal from what did surface: every retrieved system assumes the *actor owns the procedure*; enterprise BPM (procedure imposed on the actor) never appeared. Suggests the project's scope ends where procedure ownership leaves the actor — but this is an inference from absence, not evidence. If Q5 becomes load-bearing, it needs a separate targeted read (van der Aalst; Suchman's critique already lives on wizard.mdx).
