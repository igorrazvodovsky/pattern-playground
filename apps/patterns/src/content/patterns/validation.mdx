---
title: Validation
role: pattern
activityLevel: operation
atomic: pattern
mediation: individual
fun: 🥱 boring but important groundwork.
description: "Responding to input that doesn't fit — at the field where it happened, saying what is wrong and how to fix it, persisting until corrected."
situation:
  initiating: >-
    the actor has given the system something it can't accept and is about to find out on the
    system's terms; the input is already made, so prevention is behind them and what's left
    is explaining it and getting it repaired
  resulting:
    - >-
      the message sits at the field that produced it, says what is wrong and what would fix
      it, and stays there until the correction lands
    - >-
      the timing rule inverts once a field has failed: judging a first attempt mid-typing is
      punishment, and waiting for the next blur to confirm a fix is withholding
    - >-
      every message is prose somebody wrote, and a long form's worth of them is a body of
      writing that decides whether the actor recovers or abandons the form
relationships:
  instantiates:
    - to: status-feedback
      note: "the persistent, correction-demanding delivery shape of status feedback"
  hosted-by:
    - to: form
      note: "the form is the surface that hosts fieldwise messages and wires them to submission"
  complements:
    - to: data-entry
      note: "a forgiving format prevents errors this move would otherwise have to report"
  tangential:
    - to: state-disabled
      note: "disabling submit until the form is valid hides the problem this move exists to explain"
---

The actor has given the system something it cannot accept, and is about to find out on the system's terms. Validation arrives at the field where the input happened, says what is wrong and how to fix it, and stays until the correction. 

Persistence is the defining property — a message demanding action must outlive the glance, which makes validation the counterpart of [transient feedback](/patterns/transient-feedback) within [status feedback](/patterns/status-feedback)'s three delivery shapes.

## Timing

Reward early, punish late. A first-time entry should not be judged while the actor is still typing — flag it when they leave the field, or on submit. Once a field has been flagged, reverse the rule: re-validate on every keystroke, so the fix is confirmed the moment it lands rather than at the next blur.

{/* TODO — stub. The full page owes: message voice (explicit, human, precise, constructive — say the fix, not just the failure); error summary for long forms, each entry linking to its field; prevention vs correction (constraints and data entry's forgiving format remove errors this move would otherwise report); a demo; progressive help when the same field fails repeatedly. */}

## Resources & references

- [NN/g / How to Report Errors in Forms](https://www.nngroup.com/articles/errors-forms-design-guidelines/)
- [Smashing Magazine / A Complete Guide To Live Validation UX](https://www.smashingmagazine.com/2022/09/inline-validation-web-forms-ux/)
- GOV.UK / [Help users to recover from validation errors](https://design-system.service.gov.uk/patterns/validation/)