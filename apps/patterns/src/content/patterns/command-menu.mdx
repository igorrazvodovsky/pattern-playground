---
title: Command menu
role: pattern
group: seeking
activityLevel: action
atomic: pattern
mediation: individual
lifecycle: seeking
fun: 🙂 4/5.
relationships:
  complements:
    - to: autocomplete
      note: supports command menu functionality by reducing typing effort and increasing discoverability of commands.
    - to: embedded-intelligence
      note: helps the actor find relevant commands when exact matches aren't available
    - to: toolbar
      note: exposes a persistent subset of commands; a command menu offers the full set on demand
    - to: collaboration
      note: Command menus provide quick access to collaborative actions and workflows
    - to: keyboard-shortcuts
      note: "surfaces shortcuts alongside each command name, making the invisible visible — the command menu is often the primary discovery mechanism for a shortcut system"
  precedes:
    - to: unavailable-actions
      note: context menus show only relevant commands, hiding those inaccessible in the current context
  enables:
    - to: filtering
      note: provides the local search through attributes and values
---

{/* TODO: Storybook demo exists — packages/components/src/stories/CommandMenu.stories.tsx */}


The invocation of commands by name, navigating the tension between discoverability and speed.

## Global

Pop up in the middle of the screen...

## Local

Can also be used to support local functions, e.g. in [filters](/patterns/filtering) or TODO:local search.

## Related components

- <ComponentRef id="operations-combobox--docs">Combobox</ComponentRef> — the mechanism this pattern builds on; `src/components/combobox/` drives the text input, filtered list, and keyboard model
- <ComponentRef id="primitives-keyboard-key--docs">Keyboard key</ComponentRef>
- <ComponentRef id="components-context-menu--docs">Context menu</ComponentRef> — pointer-driven and target-aware; command menu is keyboard-driven and name-based

## To-do

Related patterns to capture. As precursors, a *navigation* menu establishes the baseline structure a command menu can later streamline or augment, and a command palette often extends or consolidates *keyboard shortcut* functionality, making it easier to discover. As a follow-up, *bookmarks* could pin frequently used commands for reuse.

{/* Follow-up: *action history* may streamline repetitive workflows */}
