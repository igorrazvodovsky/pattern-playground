---
title: Sorting
role: pattern
group: seeking
activityLevel: action
atomic: composition
mediation: individual
lifecycle: seeking
fun: 🙂 2/5. Dependency.
situation:
  initiating: >-
    the collection has already been narrowed to the right items but arrives in an
    order chosen once for everyone — an order that answers some question, just not
    the one this actor brought
  resulting:
    - >-
      the actor holds the order, and their choice persists, so coming back to the
      page doesn't undo the arrangement they left it in
    - >-
      order is itself a claim about what matters: whatever the collection is sorted
      by becomes the thing read first, whether or not that was the intent
relationships:
  precedes:
    - data-view
  complements:
    - to: grouping
      note: orders the members within each group
    - to: filtering
      note: orders what filtering lets through
---

Sorting allows actors to re-organize collection of items when the default order may be insufficient to scan.

<Demo name="data-view-sorting-slice" label="Sorting inside a data view" />

## Behavior

- The dropdown allows users to choose their sorting criteria.
- Sort selections are meant to be persisted. When actor returns to the page, their previous sort selections will be preserved.
- As soon as an item in the dropdown is selected, the list content re-sorts automatically.
- The default sort order is determined on a page-by-page basis.

## To-do

- Table column sorting
- Sorting using multiple attributes 

## Resources & references

- [Baymard Institute](https://baymard.com/ecommerce-design-examples/40-sorting-tool)