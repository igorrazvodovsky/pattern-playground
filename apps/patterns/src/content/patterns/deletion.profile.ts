import type { GenerativeProfile } from '../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    "a stretch of activity in which the actor wants to remove data, with consequences that range from trivial to permanent and far-reaching",
  produces:
    "a removal path matched to the severity, reversibility, and scope of the loss — confirmation, undo, soft delete, or typed challenge",
  enacts:
    "protection against accidental loss, explicit intent on consequential actions, and a recovery surface proportional to the stakes",
};
