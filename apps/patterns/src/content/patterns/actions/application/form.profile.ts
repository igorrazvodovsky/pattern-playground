import type { GenerativeProfile } from '../../../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    "a stretch of activity in which the system needs structured information from the actor that can't be inferred from prior context",
  produces:
    'a guided collection surface where fields are committed one at a time and the whole set can be reviewed before submission',
  enacts:
    "clarity about what's being asked, agency over what's submitted, room for revision before commit",
};
