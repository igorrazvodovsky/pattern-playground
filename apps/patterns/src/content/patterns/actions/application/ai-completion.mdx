---
title: "AI completion"
role: pattern
activityLevel: action
atomic: pattern
mediation: individual
---

> 🙂 **Fun meter: 4/5**. The interesting lens is seeing this as a continuation of *autocomplete* rather than an AI-specific thing.

# AI completion

Proposing [generated content](/patterns/activities/generated-content) — phrases, sentences, paragraphs, or derived values — as the actor composes, to anticipate what comes next.

Part of the [assisted task completion](/patterns/actions/application/assisted-task-completion) spectrum — the co-creative middle, where the system generates alongside the actor.
The system proposes what it *thinks* the actor wants to say, which raises [trust](/patterns/qualities/agency) and agency questions that don't come up with deterministic patterns like [autocomplete](/patterns/operations/autocomplete) or [autofill](/patterns/operations/autofill).

{/* ## When it works */}

{/* - Reducing repetitive or formulaic writing (email replies, boilerplate code, standard phrases) */}
{/* - The suggestion can be accepted with a single keystroke (Tab) and dismissed by simply continuing to type */}
{/* - Gmail Smart Compose reduces writing time by 30–40%; GitHub Copilot users completed tasks 55.8% faster — but acceptance rates sit at only 21–33%, meaning most suggestions get rejected */}

{/* ## When it fails */}

{/* - Suggestions are wrong but plausible enough to slip past uncritical review ([automation bias](/patterns/activities/bot#algorithmic-loafing)) */}
{/* - Output homogenises — AI suggestions can flatten cultural nuances and push writing toward dominant styles (Dhillon et al. 2024) */}
{/* - Calibration takes time: ~11 weeks for developers to fully realise productivity gains from AI code completion (Microsoft research) */}

## Inline completion vs. block generation

*Inline completion* continues the actor's current thought — ghost text extending the cursor position. The actor evaluates character by character whether the suggestion matches their intent. Good predictions get accepted almost automatically; divergence is instant (keep typing). Gmail Smart Compose and GitHub Copilot single-line suggestions work this way.

*Block generation* produces a discrete chunk the actor reviews as a whole — a paragraph, a code function, a reply draft. Evaluation here is more conscious: read, judge, accept or edit. This sits closer to [generated content](/patterns/activities/generated-content) than to completion, and the design shifts toward review and editing affordances.

## Design considerations

- Suggestions should feel *lighter* than the actor's own input — ghost text, reduced opacity, vanishing the moment the actor types something different. The suggestion should not compete with the actor's flow.
- Watch for [fixation risk](/patterns/actions/application/suggestion#fixation-risk): a concrete AI suggestion can narrow creative exploration. For generative tasks, question-driven scaffolding may preserve more diversity.
- Latency tolerance is higher than for [autocomplete](/patterns/operations/autocomplete) because the actor is composing, not selecting — a delay before ghost text appears is less disruptive than a delayed dropdown.
- Suppress suggestions when the model's uncertainty is high. Low-quality completions erode trust faster than silence does.

## Related patterns

### Precursors

- [Agency](/patterns/qualities/agency) — the actor remains in creative lead; the system only proposes
- [Data entry](/patterns/actions/application/data-entry) — AI completion as the AI-driven end of the input strategy spectrum
- [Autocomplete](/patterns/operations/autocomplete) — the deterministic precursor; AI completion extends the same interaction to generated content

### Complementary

- [Assistance](/patterns/foundations/assistance) — completion might assists user in *performing* and *knowing* stuff
- [Assisted task completion](/patterns/actions/application/assisted-task-completion) — the spectrum this pattern belongs to
- [Suggestion](/patterns/actions/application/suggestion) — AI completion is a suggestion mechanism; fixation risk and dismissal apply directly
- [Bot](/patterns/activities/bot) — powers AI completion via the [inline mode](/patterns/activities/bot#modes-of-interaction) of interaction
- [Generated content](/patterns/activities/generated-content) — block generation is closer to this pattern than to inline completion
- [Transparent reasoning](/patterns/activities/transparent-reasoning) — explaining *why* a completion was suggested, especially for derived values

### Follow-ups

- [Block-based editor](/patterns/actions-sensemaking-blockbased-editor) — a natural container for AI completion
- [Adaptability](/patterns/qualities/adaptation) — completion that improves from actor corrections over time

## Resources & references

- Chen et al. (2019) [Gmail Smart Compose: real-time assisted writing](https://arxiv.org/abs/1906.00080). KDD '19
- Peng et al. (2023) [The impact of AI on developer productivity: evidence from GitHub Copilot](https://arxiv.org/abs/2302.06590)
- Dhillon et al. (2024) [AI suggestions homogenize writing toward Western styles and diminish cultural nuances](https://arxiv.org/abs/2409.11360)
- Maier, Schneider, Feuerriegel (2025) [Partnering with Generative AI: experimental evaluation of human-led and model-led interaction in human-AI co-creation](https://arxiv.org/abs/2510.23324)
- Shape of AI – [Auto-fill pattern](https://www.shapeof.ai/patterns/auto-fill)
