import type { GenerativeProfile } from '../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn: 'a moment where the actor must designate members of an exposed set of options',
  produces:
    'a designation — narrowed by scanning or typing — whose fate (committed value, staked target, spliced text) is set by the surrounding flow',
  enacts: 'recognition over recall, legibility of what can be picked, economy of designation',
};
