---
title: Toolbar
role: pattern
group: coordination
activityLevel: action
atomic: composition
mediation: coordination
lifecycle: coordination
fun: 🙂 3/5. Dependency.
relationships:
  precedes:
    - to: data-view
      note: the toolbar provides the persistent surface where the view's collection controls live
    - to: filtering
      note: the toolbar hosts the filter control the move launches from
    - to: grouping
      note: the toolbar hosts the group-by control the move launches from
    - to: sorting
      note: the toolbar hosts the sort control the move launches from
  related:
    - to: unavailable-actions
      note: the pattern behind hiding toolbar items that are inaccessible in the current context
  complements:
    - to: command-menu
      note: a keyboard-driven complement; exposes the full command set on demand where the toolbar exposes a persistent subset
  enacts:
    - to: agency
      note: puts the relevant actions within reach, surfacing the ones that fit the actor's current intent
---

{/* TODO: Storybook demo exists — packages/components/src/stories/Toolbar.stories.tsx */}


The persistent exposure of frequently used commands, balancing quick access with visual economy.

## Context

Options presented in the toolbar can depend on the context and state of the parent component. Things that are not applicable in the parent’s state can be hidden. For example, if the parent is empty the user will have different needs compared with a full state.

## Order

Things inside toolbars are consistently ordered so that users can easily find things across different features and applications.

Features should be sorted in order of importance. Secondarily, features should be organized in logical groupings for easier recall.

{/* ## Item groupings */}

{/* - Start:  */}
{/* - End: important items that need to remain available, an optional search field, and the More menu that contains additional items. It also includes a primary action when one exists. */}

## Related components

- <ComponentRef id="components-action-bar--docs">Action bar</ComponentRef> — the transient counterpart; summoned by selection rather than persistent, and typically composes a toolbar internally for its action group
- <ComponentRef id="components-priority--docs">Priority+</ComponentRef> — handles overflow when toolbar items don't fit the available space
