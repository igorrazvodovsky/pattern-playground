import type { GenerativeProfile } from '../../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    'a free-form input where the set of valid answers is large or the actor is still formulating the query',
  produces:
    "a narrowing surface of candidate completions running alongside what's been typed",
  enacts:
    'assistance without taking over, discoverability of options that exist, economy of typing',
};
