---
title: "Unavailable actions"
role: pattern
activityLevel: operation
atomic: pattern
mediation: individual
fun: "🙄 boring but important groundwork."
---
Hide actions that are currently inaccessible or irrelevant to the actor.

## Examples

Context menus & toolbars: only displaying actions relevant to the current context rather than disabling irrelevant items.

Permission-based features: completely removing actions actors lack permissions for, rather than showing them disabled.

## Edge cases

Situation when actors need to understand the full range of potential actions to establish a clear mental model.

## Accessibility concerns

Removing UI elements entirely (e.g., changing layout structure) might negatively impact assistive technology.

## Related patterns

- Hiding options is an alternative to [disabled state](/patterns/operations/state-disabled)
- [Morphing controls](/patterns/operations/morphing-controls) — keeps the control visible but transforms it
- [Progressive disclosure](/patterns/actions/seeking/progressive-disclosure) — a softer form: deferring rather than removing
- [Toolbar](/patterns/actions/coordination/toolbar) — primary context where actions are hidden based on selection or state
- [Command menu](/patterns/actions/seeking/command-menu) — context menus apply the same principle, surfacing only relevant commands