---
title: Prompt
role: pattern
activityLevel: activity
atomic: pattern
mediation: individual
fun: 🙂 5/5. A part of a bigger attempt to map LLM-related patterns.
situation:
  initiating: >-
    the actor holds a need in the loose form they think in, and the system can act on
    language — so the work is turning a vague want into a request precise enough to be
    acted on, with no syntax standing between the two
  resulting:
    - >-
      intent is stated in the actor's own words, and initiative stays with whoever framed
      the request rather than with whoever built the interface
    - >-
      what comes back depends on what the request happened to carry, so the actor is now
      responsible for context they may not know is missing
    - >-
      the request is a durable artefact — keepable, editable, shareable, templatable —
      which makes a good one worth more than any single thing it produced
relationships:
  precedes:
    - to: agent
      note: the prompt articulates the request the agent acts on
    - to: generated-content
      note: the input that leads to generated content.
    - to: ai-tuning
      note: lets the actor refine how the model gets prompted
  enables:
    - to: conversation
      note: the prompt is the turn-level move a conversation with an AI is built from
  enacts:
    - to: agency
      note: the actor directs the system in their own words; initiative stays with whoever frames the request
---

The articulation of intent to a machine, transforming a vague need into a request precise enough for an [agent](/patterns/agent) to act on meaningfully.

<Demo name="prompt-basic" label="Structured prompt composer" />

## Intent

explicit and implicit...

## Input mechanisms

### Structured prompt

<Demo name="prompt-template" />

## Reference materials with @mentions

Using @mentions to reference documents, images, code, and other materials provides context for better LLM responses.

<Demo name="prompt-with-material-references" label="Prompt with @-referenced materials" />

## Prompt guidance and scaffolding

Bridging the gap between fuzzy idea inside a human's head and what makes an effective prompt for LLMs.

### Quality feedback

<Demo name="prompt-quality-feedback" label="Prompt quality feedback" />

### Editing assistance

### Templates

...

### Suggestions

...

### Prompt evaluation

...

### Reflective prompting

{/* TODO: Here or in suggestions+link */}
Real-time, context-aware prompt completions derived dynamically from users’ immediate and historical interactions.

{/* TODO: relations set aside, kept for review (not active edges):
Complementary patterns:
- LLM model selection
- [Messaging](/patterns/components-messaging)
- [Suggestion](/patterns/suggestion)
*/}

## To-do

Precursors to capture: *session*, *thread*, and the other conversational containers a prompt sits within.

## Resources & references

- [shapeof.ai](https://www.shapeof.ai/)
- [aiuxpatterns.com](https://aiuxpatterns.com/)