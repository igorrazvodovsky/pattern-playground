import type { GenerativeProfile } from '../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    "a value the actor must supply from a set they didn't author and may not be able to survey",
  produces:
    'a visible, narrowable candidate set that converges on a valid committed value',
  enacts: 'vocabulary legibility, integrity of what gets committed',
};
