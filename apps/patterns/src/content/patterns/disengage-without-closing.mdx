---
title: Disengage without closing
role: pattern
group: conversation/conversation-management
activityLevel: operation
atomic: primitive
mediation: coordination
fun: 😊 3/5. Looking for useful building blocks for conversational UI.
situation:
  initiating: >-
    the exchange is mid-activity and one party has to step away — idle,
    interrupted, a dropped connection — with the goal still unmet; a formal close
    would misstate where things stand, and dropping it silently loses the work
  resulting:
    - clause: >-
        the exchange is parked rather than ended: state is saved, the actor can see
        what persists and for how long, and either side can pick it up again — the
        agent resuming, or the actor returning on their own initiative
      sets-up: [agent-opening, user-opening]
    - >-
      parked state ages while nobody is watching it, so the assumptions inside it
      may not hold on return; confirming them is part of resuming, not an extra
relationships:
  enables:
    - conversation
  alternative:
    - to: closing
      note: the preferred alternative when a formal close is possible
---
Allow either party to step away without a formal close (e.g., idle, context switch), keeping state ready for later resumption.

1. U or A: Disengage trigger (idle, multitask, network drop, “hold on”).
2. A: Non-intrusive acknowledgement; save state and provide a resume affordance (e.g., “Pick up where we left off”).
3. A (Optional): Set a gentle reminder or time-bound expiration (with consent).
4. U: Return later → resume at the same step or start a fresh opening.

- Avoid pushy re-engagement; prefer user-initiated resume.
- Show *what’s saved* and how to *change or discard* it.
- For privacy, clearly state what persists and for how long.

## Support & transitions
- On resume, confirm *assumptions/state* and offer to continue or start over.
- If state is stale, propose *opening* re-open or an updated *scope*.

## Metrics
Successful resumes; time-to-resume; stale-state errors; opt-outs of reminders.
