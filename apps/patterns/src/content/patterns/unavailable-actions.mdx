---
title: Unavailable actions
added: 2025-03-16
updated:
role: pattern
activityLevel: operation
atomic: pattern
mediation: individual
fun: 🙄 boring but important groundwork.
situation:
  initiating: >-
    an action exists in the surface's vocabulary but doesn't apply to what the actor is
    looking at, and the alternatives have been weighed: showing it inert puts a thing they
    can't use in front of them, and this isn't an action they need to know about here
  resulting:
    - >-
      the surface carries only what applies, so nothing the actor can see is unusable
    - >-
      an action never shown is an action the actor may not know exists, which puts the full
      range of what's possible somewhere other than the current context or nowhere at all
    - >-
      hiding changes the layout, so the same surface has a different shape in different
      states — which spatial memory and assistive technology both read as things moving
relationships:
  alternative:
    - to: state-disabled
      note: hiding options is an alternative to showing them disabled
  related:
    - to: progressive-disclosure
      note: "a softer form: deferring rather than removing"
---

Hide actions that are currently inaccessible or irrelevant to the actor.

## Related components

Context menus & <ComponentRef id="components-toolbar--docs">toolbars</ComponentRef>: only displaying actions relevant to the current selection or state rather than disabling irrelevant items.

## Edge cases

Situation when actors need to understand the full range of potential actions to establish a clear mental model.

## Accessibility concerns

Removing UI elements entirely (e.g., changing layout structure) might negatively impact assistive technology.
