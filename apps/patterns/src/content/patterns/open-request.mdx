---
title: Open request
added: 2025-10-17
updated:
role: pattern
group: conversation/conversational-activities
activityLevel: operation
atomic: primitive
mediation: coordination
fun: 😊 3/5. Looking for useful building blocks for conversational UI.
situation:
  initiating: >-
    the actor has stated a goal rather than a step — complex enough that the
    agent can't act on it as given, with constraints missing and several moves
    between the request and the outcome
  resulting:
    - >-
      the request is framed rather than merely received: goal, constraints,
      assumptions and sources are explicit, and what's still missing shows up as
      named gaps instead of general vagueness
    - clause: >-
        a plan exists and the agent is about to act on the actor's behalf; where
        a step can't be taken back, the actor's explicit go-ahead has to come
        before it, not after
      sets-up: [inline-confirmation]
relationships:
  enables:
    - to: conversation
      note: the primary primitive for instruction-based dialogue
  composed-of:
    - to: inquiry-agent
      note: used within open request to gather missing or ambiguous constraints
---
A user makes a complex request that typically requires clarification and/or multiple steps.

1. U: Complex request ("Configure a pump for 120 m³/h at 8 bar").
2. A: Establish scope + ask for constraints (→ *agent inquiry*).
3. A: Plan/confirm next action ("I will size the pump using… Shall I proceed?").
4. A: Execute or transition to *extended telling* (instructions) as needed.
5. U (Optional): Sequence closer.

## Guidance
- Show a brief intent frame, e.g. goal, constraints, assumptions, sources, risk.
- Prefer *mixed-initiative* clarifications; ask only for missing/ambiguous slots.
- Gate irreversible actions behind an explicit *confirmation*.

## Repair affordances
- One-tap: Repeat · Say it simpler · Example · Define term.
- Support *retroactive repair* (“Apply this correction to the last step”).

## Metrics
- Wrong-action rate before confirmation
- Clarifications per task; completion time
- Accuracy against source-of-truth/tool outputs
