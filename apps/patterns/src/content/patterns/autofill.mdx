---
title: Autofill
added: 2026-03-25
updated:
role: pattern
activityLevel: operation
atomic: pattern
mediation: individual
fun: 🙂 2/5. Dependency.
situation:
  initiating: >-
    the values the fields need are ones the system already holds — from the actor's profile, a
    prior session, the browser — so asking them to type what is already known is work with no
    purpose; and the confidence is high enough that filling won't be wrong
  resulting:
    - >-
      the fields arrive already populated from the actor's own data, and what's left for them
      is checking rather than typing
    - >-
      filling a field silently shows the actor what the system knows about them, in a place
      they hadn't asked the question
    - >-
      pre-filled values get submitted without being read, so a wrong stored value travels
      further than a wrong typed one would have
relationships:
  complements:
    - to: autocomplete
      note: "the reactive counterpart: completes what the actor is actively typing rather than pre-populating from stored values"
    - to: assisted-task-completion
      note: the spectrum this pattern belongs to
    - to: suggestion
      note: autofill as a form of implicit recommendation
  enacts:
    - to: privacy
      note: autofill surfaces what the system knows about the actor
    - to: adaptation
      note: autofill that learns from corrections over time
  enables:
    - to: data-entry
      note: autofill as a strategy for frictionless input
  hosted-by:
    - to: form
      note: form fields are the surface where autofill most commonly appears
  precedes:
    - to: saving
      note: what happens after pre-filled data is accepted
---
Populating fields with previously known or stored values, pulled from actor profiles, prior sessions, browser data, or contextual defaults.

Part of the [assisted task completion](/patterns/assisted-task-completion) spectrum. Preemptive, silent, and only useful when confidence is high.

{/* ## When it works */}

{/* - The data is structured and repetitive: addresses, payment details, profile fields */}
{/* - Stored values are almost always correct */}
{/* - Pre-filled values look different from typed ones (Chrome's yellow highlight, for instance) so the actor can tell what came from the system */}

{/* ## When it fails */}

{/* - Pre-populated data is wrong, and deleting before correcting costs more effort than typing from scratch */}
{/* - The system fills fields the actor didn't expect it to know about, raising [privacy](/patterns/privacy) questions */}
{/* - Hidden fields get silently filled and read via JavaScript before submission. This is the autofill dark pattern (Friedman 2021) */}

## Data sources

### Browser

Address, payment, and identity data that the actor manages through browser settings. The actor controls what gets stored; the browser matches fields by name attributes and autocomplete hints.

### System

The application fills fields from its own knowledge of the actor: account details, preferences, prior entries. Less transparent than browser autofill because the actor may not realise what the system has on file.

## Related components

- <ComponentRef id="components-input--docs">Input</ComponentRef> — the primitive autofill populates

## Resources & references

- Friedman (2021) [The autofill dark pattern](https://www.smashingmagazine.com/2021/10/autofill-dark-pattern/). Smashing Magazine
- MDN [HTML autocomplete attribute](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Attributes/autocomplete)
