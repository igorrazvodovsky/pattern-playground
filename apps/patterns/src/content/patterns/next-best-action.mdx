---
title: Next-best action
added: 2026-03-25
updated:
role: pattern
group: application
activityLevel: action
atomic: pattern
mediation: individual
fun: 🙂 4/5. Connects workflow guidance with agentic AI.
situation:
  initiating: >-
    the actor is at a transition point in a workflow the system can read — it knows
    what they just finished and holds enough signal to name a plausible next step — and
    what's worth proposing is a whole action rather than a value, so nothing further
    down the completion spectrum reaches it
  resulting:
    - clause: >-
        the system proposes actions rather than values, so accepting one commits the
        actor to something whose consequences they didn't work out themselves; what
        guards the accept has to be proportional to what the action does
      sets-up: [action-consequences]
    - clause: >-
        where the stakes are high, the recommendation being easy to accept is the
        problem rather than the feature — the accept needs a beat of deliberate thought
        built into it
      sets-up: [cognitive-forcing-functions]
    - >-
      recommendations now arrive uninvited, at moments the system picked; a pile of them
      nobody resolved teaches the actor to stop reading the channel at all
relationships:
  serves:
    - to: assistance
      note: Next-best action draws on assistance to decide *what* to suggest at workflow transitions
    - to: delegation
      note: Next-best action sits at delegation's accept-or-perform boundary — the same recommendation can be presented for the actor to accept or for the agent to perform.
  enacts:
    - to: agency
      note: the system proposes the step; the actor takes it or leaves it
    - to: adaptation
      note: recommendations that improve from accepted/rejected signal over time
  complements:
    - to: assisted-task-completion
      note: the spectrum this pattern belongs to
    - to: agent
      note: 'powers next-best action via the [ambient mode](/patterns/agent#modes-of-interaction) of interaction'
    - to: embedded-intelligence
      note: next-best action as intelligence woven into the workflow
    - to: transparent-reasoning
      note: explaining the rationale behind recommendations
    - to: notification
      note: a common delivery mechanism for next-best action prompts
    - to: onboarding
      note: next-best action as guided first-use experience
---
The system analyses what the actor is doing and recommends the most useful next step.

The most forward-leaning pattern on the [assisted task completion](/patterns/assisted-task-completion) spectrum, right at the edge of autonomous action. It works at the *workflow level*, not at the field or content level, and suggests *actions* — schedule a call, run tests, send a follow-up — rather than data values or text.

{/* ## When it works */}

{/* - The workflow has clear sequential steps and the system has strong contextual signals: CRM after logging a call, IDE after editing code, onboarding flows */}
{/* - Recommendations appear at natural *transition points* between workflow steps, not mid-task */}
{/* - The suggestion includes a brief rationale. Actors need to understand *why* this action is recommended, or they won't act on it */}

{/* ## When it fails */}

{/* - Recommendations feel presumptuous or interruptive. This is the "annoying assistant" trap, and it's easy to fall into */}
{/* - The system doesn't have enough context to suggest anything useful */}
{/* - Suggestions pile up without resolution, and actors start ignoring all of them */}
