import type { GenerativeProfile } from '../../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    'an evolving exchange between actors where meaning has to be built up over multiple turns rather than stated in one',
  produces:
    'a dialogue structure that carries context forward, lets either party take a turn, and accommodates repair when alignment slips',
  enacts: 'shared understanding, mutual agency, room for the unfolding nature of meaning',
};
