import { Meta } from '@storybook/blocks';

<Meta title="Foundations/Spacing" />

# TODO: Spacing

- The spacing is structured around a base unit for each density. It determines the scale used across the design system.
- T-shirt sizes are used for spacing. This makes it possible for anyone to understand the differences and how they relate to each other.
- 4px is a default space (M) in medium (M) density.