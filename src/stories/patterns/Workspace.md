import { Canvas, Story, Meta, Subtitle } from '@storybook/addon-docs/blocks';

<Meta title="Patterns/Workspace" />

# Workspace

A place where users can view more than one page, project, file, or context at a time.
It can consist of tabs, streams/feeds, panels, or windows.
Users might have the option to place these workspaces side by side.


