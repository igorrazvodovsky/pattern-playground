import type { GenerativeProfile } from '../../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    "an actor's first encounter with a system (or a changed part of one) before they have a working mental model of what's possible and what will happen",
  produces:
    'a bounded period in which the actor can build that model with low stakes, often by doing rather than reading',
  enacts:
    "learnability of the system's affordances, safety to try things, value visible early enough to motivate continued use",
};
