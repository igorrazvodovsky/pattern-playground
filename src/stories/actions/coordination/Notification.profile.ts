import type { GenerativeProfile } from '../../../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    "a system event whose existence the actor needs to know about, alongside an existing flow that has its own demands on attention",
  produces:
    "a delivery surface tuned to the event's urgency and the actor's availability",
  enacts:
    "awareness without disruption — or with disruption when warranted — and respect for the actor's current attention",
};
