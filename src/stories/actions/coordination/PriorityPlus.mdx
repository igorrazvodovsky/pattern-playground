import { Canvas, Story, Meta } from '@storybook/addon-docs/blocks';
import * as PriorityPlusStories from './PriorityPlus.stories.tsx';

<Meta title="Actions/Coordination/Priority+" of = { PriorityPlusStories } tags={['activity-level:action', 'atomic:component', 'lifecycle:coordination', 'mediation:coordination']} />

> 🥱 **Fun meter: not very captivating by itself**. Included as a prerequisite for more interesting things.

# Priority+

The allocation of limited space to competing controls, balancing immediate access against visual simplicity.

<Story of={ PriorityPlusStories.PriorityPlus } />

Priority+ implements a [pattern](https://bradfrost.com/blog/post/complex-navigation-patterns-for-responsive-design/#priority-plus) of the same name: show elements that fit the container, tuck everything else behind a dropdown. Built specifically for making other components responsive — almost every usage involves building other components rather than appearing directly in markup.

## Supported components

By default Priority+ will move the element to `list` as is but certain components are transformed along the way. E.g. `button` becomes `list-item`.

## Related patterns

- Depends on a [dropdown](../?path=/docs/actions-coordination-dropdown--docs) as a place to hide things in and a [list item](../?path=/docs/actions-evaluation-list--docs) for element transformation.
- [Toolbar](../?path=/docs/actions-coordination-toolbar--docs) — the primary consumer. Priority+ makes toolbars responsive
- Priority+ is a way to handle [overflow](../?path=/docs/operations-overflow--docs) for list-like components.

## TODO:
- Placing things under dropdown by default. It might happen that user might need to hide certain things under the dropdown anyway.
- Exceptions?
- Component support
  - toggles → list items + checkbox
  - select → sublist
  - inputs?