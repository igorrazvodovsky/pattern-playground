# Tech debt tracker

- *Two quality→foundation edges leak past the purist quality stance* — `privacy enables collaboration-foundation`, `learnability tangential localization`; await a decision. Surfaced at the typed-relationships close (2026-06), see `plans/completed/2026-06-typed-relationships.md`.
- *View-system reshape leftovers* (close 2026-07-25): pill-row visual once-over still owed; `RepresentationType: 'card'` naming question open ('grid' rejected — ARIA grid and Airtable grid both mean table); smooth fisheye animation across grain boundaries needs a flat all-grains-mounted renderer (fidelity, not correctness).

- *58 remaining `tsc` errors* (2026-08-15, after a sweep that cleared 215 of 273). The root project, `apps/server`, `shared/`, and the Astro layer are clean, so all 58 sit in `packages/components`; `apps/patterns` reports a subset of the same errors, reached through its imports. What's left needs decisions, not edits: (a) 25 across the commenting subsystem, where `QuoteObject` and `CommentPointer` each exist twice with different shapes (`services/commenting/quote-service` vs `services/commenting/core/quote-pointer`) — picking the canonical one is a design call; (b) 21 tracing back to two parallel `SearchableParent`/`SearchableItem` definitions (`utility/hierarchical-search` vs `hooks/useHierarchicalNavigation`) that differ on `icon` type and index signature, reaching command-menu, `ReferencePicker`, and the filter demos; (c) 5 in `EditorProvider`/`PerformanceMonitor`, where the context object supplies values where the type wants thunks — read as a live bug, not a type nit; (d) 1 where `EditorFloatingMenu` imports `FloatingMenu` from `@tiptap/react`, which no longer exports it in Tiptap 3 — needs `@tiptap/extension-floating-menu` or a rewrite; (e) 6 scattered singles (`aiFilterAdapter`, `ReferencesPlugin` toolbar props, `TemplateField` node-view props, `list.ts` composed path).
- *117 TODO/FIXME comments across 61 files*. No ownership or dates on most. Quarterly review process recommended but not yet established.
- *Residual Atomic Design stubs in `src/stories/patterns/`* — `.md` stub files (Autofill, Checklist, etc.) left behind after AT reorg. Not served by Storybook. Low priority. See `plans/completed/2026-03-activity-theory-reorg.md`.
- *CSS TODO comments* — inline TODOs in CSS files noted in `plans/completed/2025-codebase-review-recommendations.md` §5.
- *`no-explicit-any` eslint-disable in 4 files* — `Reference.tsx` (Tiptap types), `useDragToCreate.ts`, `nodeTypes.tsx`, `NodeShapeUtil.tsx` (tldraw types). Added 2026-04-12 during harness refactor ESLint enforcement.
- *`no-unused-vars` pre-existing errors (36)* — scattered across tldraw, components, and services. Not introduced by harness refactor. See `npm run lint` output.
- *2 inline `style` prop eslint-disables* — `CommentComposer.tsx` (flex layout), `OnCanvasComponentPicker.tsx` (dynamic width). Added 2026-04-12.
- *Dual listbox needs an implementation* — `packages/components/src/stories/DualListbox.mdx` is a seed stub. Add component + Storybook story
- *Interaction behaviours are not graph-addressable* — every navigation pattern's behavioural-position section links `/patterns/interaction#…` anchors (navigating, browsing, exploring, re-finding…), so the model↔behaviour relationships exist only as body links invisible to the typed graph. Needs anchor-level addressability or behaviour nodes. Surfaced on 2026-07, workspace-split closure; already flagged page-locally in navigation-overview's To-do. Shares a substrate with the hybrid-patterns split question (sub-page graph addressability) — see the closure plan's T1 inputs.
- *Select dropped its prefix/suffix slots undecided* — the shadow select styled slotted adornments; `select.ts` and `select.css` now have no trace of them, while input, range, and list-item all kept the `data-slot="prefix"/"suffix"` contract. Nothing in the repo uses select adornments, so nothing is broken — the capability disappeared silently rather than being decided away. Either restore the contract for parity or record the removal as deliberate.
