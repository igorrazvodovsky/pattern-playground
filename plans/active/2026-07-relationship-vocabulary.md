---
title: "Relationship vocabulary: situations, part–whole hygiene, and the hosting gap"
status: "active"
kind: "exec-spec"
created: "2026-07-08"
last_reviewed: "2026-07-10"
area: "language, graph, pattern-site"
promoted_to: ""
superseded_by: ""
depends_on: "plans/completed/2026-06-typed-relationships.md, plans/completed/2026-07-workspace-split-closure.md (workstream 2 gates the realised_by item)"
---
# Relationship vocabulary: situations, part–whole hygiene, and the hosting gap

The workspace-split territory closures generated vocabulary findings faster than any plan owned them: each closure's log recorded an input "for the relationship-vocabulary work" and moved on. This plan is that work's home. It owns the accumulated items, sorts them by what they actually are (one hygiene audit, one construct decision, two gated verdicts), and carries the research gate the construct decision needs before anything is committed to [relationship-vocabulary.md](../../docs/language/relationship-vocabulary.md).

The vocabulary doc stays the record; this plan is the vehicle. Changes land there through its changelog, per its own convention.

## Inventory

Each item marked with provenance. All originate from the closure plan's progress log or open questions unless noted.

1. *Part–whole misuse in the conversational cluster.* The 13 conversational primitives point at `conversation` through two different relations — 9 `enables`, 4 `instantiates` (abort, extended-telling, inquiry-user, user-repair) — with no discernible principle behind the split. The vocabulary doc settled this distinction (component–integral vs genus–species, after Winston et al.); by its own definitions the four `instantiates` are wrong: an abort is a constituent move *within* a conversation, not a *kind of* conversation. The definitions are settled but nothing at authoring time surfaces them. (T3 post-review repair, 2026-07-08.)
2. *Situations on edges.* Three sightings of one construct:
   - A conditional edge type: flat-navigation → hub-and-spoke reads as `precedes` when item count grows and as `alternative` when it is fixed; the note field carries the condition today. (T1 input, 2026-07-02.)
   - The graph already holds two same-typed edges between one pair distinguished only by `situationalHints` (notification's tree emits two `recommends` to transient-feedback). (T5 reflection, 2026-07-02.)
   - Two prose decision trees waiting to emit edges: action-consequences (whose three dimensions — time to recover, scope of impact, cascade effects — are the natural type system for consequence properties) and status-feedback (initiation, action required, urgency). (T5 addendum and feedback-cluster entry.)
   - The deletion→undo bundle: one page carries the same situational judgement in three carriers — `precedes: undo` (the sequence claim), `recommends: undo` with tree hints (when it applies), and `related: action-consequences` (how to judge the consequence). The strongest single-page specimen: whatever construct B lands should let these read as one thing. (Workstream A traversal review, 2026-07-10.)

   The stated convergence: decision-tree branches, edge conditions, and consequence/resulting-context properties are one construct seen from three ends; a conditional `precedes` decomposes into the join between A's resulting context and B's initiating situation. Accumulated situation awareness during traversal is what the construct ultimately serves.
3. *The hosting gap* (closure plan open question 7). No relation expresses *where a move's trigger is surfaced*: toolbar hosts the group-by control that launches grouping, which is neither `precedes` (generative sequence) nor `enables` (dependency). The cheapest repair — glossing notes on the existing `precedes` edges — is applied and holding.
4. *`enables` vs `realised_by`* (closure plan workstream 2, last bullet). Once component references resolve against Storybook's `index.json`, the pattern→component edge can leave the graph and become a cross-dataset reference, letting `enables` mean only move-composition within the language. Gated on the validator landing; the vocabulary decision is this plan's, the validator is not.
5. *The note field is doing three jobs* — gloss, condition carrier, and disambiguation (the conversational primitives needed notes specifically so that *Closing* in a mixed list isn't read as window chrome). Symptom, not item: when situations get their own construct (item 2), conditions leave the notes; the audit in workstream A should not "clean up" condition-bearing notes before then.

## Shape

Items 2, 3, and 5 are one family: the vocabulary lacks a first-class way to say *in what situation this edge applies*, so conditions hide in notes, situational hints exist only on unauthorable `recommends` edges, and the hosting relation can't say "the trigger lives there". Item 1 is hygiene against an already-settled spec. Item 4 is a boundary redraw, already decided in direction and merely gated. The plan therefore has one reflective decision (workstream B) and three mechanical-once-decided moves around it — the same reflective/mechanical split the closure plan's process note prescribes.

## Workstream A — part–whole hygiene (executable now)

- Re-type the four conversational `instantiates` → `enables` (their reverse notes on conversation.mdx move from `instances:` to `composed-of:` accordingly).
- Audit the remaining `instantiates` edges (8 after the fix: assisted-task-completion→suggestion, bounded-choice→selection, collaboration→collaboration-foundation, learnability→agency, wizard→step-by-step, transient-feedback→status-feedback, user-opening→inquiry-user, validation→status-feedback) and the 31 `enables` edges against the doc's definitions. Most look right on a first pass; user-opening→inquiry-user and learnability→agency (quality→quality) are the two worth actual thought.
- Extend the extractor's existing axis sanity advisory with a mixed-cluster check: flag any node that is the target of both `enables` and `instantiates` from entries sharing a `group`, which is exactly the conversational-cluster signature. Advisory, not error — the epistemic stance says hints, not predicates.
- *Notes-voicing audit (added 2026-07-08, T4 follow-up).* A single-sided note renders on both endpoints' pages, always after the *other* endpoint's name — so a subjectless note binds to whichever endpoint the reader is not on. ~211 directed edges plus the single-noted symmetric set fall back this way. Most read fine because they gloss the relation; the failure class is a note voiced for one page (e.g. data-view→selection's "staking a subset…" read as a description of data-view; toolbar's hosting notes read as the *move* doing the hosting on toolbar's own page). Audit rule per edge: reword to a relation-gloss or subject-naming form, or author the reverse note via the inverse alias / near-side note; the worst six were fixed inline 2026-07-08 (CFF→agency, help→annotation, collaboration→collaboration-foundation, conversation→conversation-quality, toolbar's four hosting notes, plus selection's reverse note on data-view's `enables`). A cheap advisory could flag candidates mechanically: directed edges whose note lacks both endpoints' names.

## Workstream A′ — precedes sweep (executable now, second sitting)

The 2026-06-24 changelog entry promised a corpus sweep of `precedes` edges for `Precursors`-heading mistypings and it never ran. Workstream A's voicing pass surfaced a live case: `searching precedes command-menu` glosses "searching is the broader seeking move" — a taxonomic claim wearing a sequential type. Sweep the 91 `precedes` edges against the generative-sequence definition ("applying A produces a centre on which B acts"); mistypes will mostly want `instantiates` or `complements`. Same character as A: hygiene against settled definitions, one sitting, changelog entry.

## Workstream B — situation constructs (strawman, research-gated)

Strawman to be broken, not built: edges gain an optional *condition* (the situation in which the edge applies, typed against the dimension vocabulary action-consequences already exhibits); patterns gain *resulting-context* (and possibly *initiating-situation*) properties; `situationalHints` on `recommends` becomes the general condition mechanism rather than a decision-tree-only artifact; prose decision trees become authorable sources of conditioned edges rather than a parallel format. Deliverable here is schema and authoring rules only — what is authorable, in which channel, and where notes end and conditions begin.

Before committing: the external research gate, framed as *what would this design be wrong about*. Candidate breakage questions: whether conditions on suggestion-grade edges inevitably drift rule-grade (the doc's epistemic stance is the constraint to defend); what condition-bearing-edge prior art says (decision modelling à la DMN, situation calculus, Alexander's sequences and their unfolding conditions, guard conditions in statecharts); whether resulting-context belongs on the node or on the edge (the same fact is authorable in both places — pick one before two authors pick differently); and whether the dimension vocabulary should be controlled at all, given the doc's own warning against premature tag normalisation.

Added after A′ (2026-07-10): *which register the vocabulary speaks in.* A `precedes` claim can be read as the designer's traversal (the doc's generative-moves commitment: applying move A produces the centre move B acts on) or as the actor's runtime sequence (the interaction unfolds A then B). The sweep found the two coincide on nearly every kept edge — the runtime sequence is usually the *evidence* for the design-time claim — and the divergent residue is exactly the conditional-escalation family this workstream owns (defaults → completion assists, flat-navigation → hub-and-spoke: B replaces A under a condition rather than acting on A's product). The strawman should state which register is primary and how the other relates; note that resulting-context/initiating-situation is the construct where the two naturally reconcile (a runtime situation A produces *is* the design-time centre B addresses).

### Research gate result (2026-07-10)

Full findings in [research/situation-constructs/2026-07-10.md](../../research/situation-constructs/2026-07-10.md). The strawman element by element:

- *Edges gain an optional condition* — *amended*. Prior art (BPMN→DMN, statechart guard explosion) says per-edge authorable conditions scatter one judgement across carriers — the deletion→undo bundle and notification's twin `recommends` are the in-graph proof. The judgement gets one authorable home (the node's resulting-context/initiating-situation, or the decision tree) and edge-side condition text is a *rendering* of that home, never a second place to author the fact.
- *Patterns gain resulting-context (and possibly initiating-situation)* — *upheld*, unanimously: Meszaros & Doble's pattern form, PLML's `resulting-context` element (whose `pattern-link` has no condition slot), and Alexander's closing links all put both situations on the node. Initiating-situation is equally form-canonical (context "described via a 'situation'… in terms of the patterns that have already been applied").
- *`situationalHints` becomes the general condition mechanism* — *upheld in shape, amended in scope*. Its `{question, branch}` shape is the right one (controlled dimension, free value), but hints stay attached to the judgement home (the tree) rather than becoming a free-floating field on arbitrary edges.
- *Typed against the dimension vocabulary* — *broken*, by the proliferation corpus (Trigg, Shipman & Marshall, adaptive hypermedia's rule-language authoring collapse) and by Schmidt's modellability criterion: no corpus-wide controlled condition vocabulary. Each tree owns its dimensions as authored questions; values stay prose; convergence is normalised post hoc through the changelog.
- *Prose decision trees become authorable sources of conditioned edges* — *upheld and strengthened*: the trees are exactly the first-class decision home the prior art converged on (DMN's separation of concerns), so this element is the mechanism that keeps conditions from scattering.
- *The epistemic stance* — defensible, with the defence relocated: drift to rule-grade happens at *evaluation*, not at structuring. The construct needs a stated consumer contract — no pipeline step may match, filter, or route on a condition; conditions render as prose for judgement. (Contradiction logged: references/report-pattern-language-formats.md recommends queryable postconditions; the stance governs.)
- *Register (post-A′ question)* — answered: designer traversal is primary; three traditions define a situation as the history of moves (situation calculus's `do(a,s)`, pattern-form context, Alexander's unfolding). The runtime sequence is evidence, and register *divergence is the detector*: an edge the two registers read differently (the escalation ladder) is a compressed conditional join — decompose it through the node constructs rather than re-typing it.
- *Workstream C input* — hosting is *spatial inclusion*, which Winston, Chaffin & Herrmann explicitly exclude from meronymy; that is why every shunt into `enables` lost part of the authored claim. Hosting is a distinct relational family; edge type vs resulting-context clause remains the schema sitting's choice, but continued absorption into `enables` is evidenced against.

Riders not reached: negative-space marking and quality support/tension drew nothing — both stay open, neither is nearly free.

Left for the schema sitting: field names and shapes for the node-side situations (and whether an edge carries a derived condition string or renders from the join at build time); the authoring channel for trees as conditioned-edge sources, and migration of the held specimens (good-defaults→autofill, live-presentation→generated-content, searching→navigation-overview); whether the escalation-ladder `precedes` edges decompose now or stay held, and the Autocomplete-instantiates-Good-defaults gap; the hosting resolution (edge type vs B-construct, with the spatial-inclusion datum); the consumer contract's wording in the doc's epistemic stance; and the changelog entry recording all of it.

## Workstream C — the hosting relation (decide inside B)

Three candidate resolutions: keep the glossing notes (status quo); mint a `hosts` edge type; or express it through B's constructs (the trigger's location as part of the hosting composition's resulting context). Do not mint an edge type before B settles — a hosting edge added now would be the third relation whose semantics B's constructs would then partially absorb.

*Evidence update (2026-07-10, A′).* The hosting shape recurs well beyond toolbar: the precedes sweep hit four more hosting-flavoured notes — ai-completion in block-based-editor ("a natural container"), autocomplete and autofill in form ("where it most commonly lives" / "the container where it appears"), onboarding on state-empty ("the canvas") — and shunted them into `enables`/`complements`, each time losing part of the authored claim. Whatever resolution lands should treat hosting as a corpus-wide relation, not a toolbar special case.

## Workstream D — `realised_by` (gated on the index.json validator)

When the closure plan's workstream-2 validator lands, decide the split: `enables` stays within-graph for move composition; component realisation becomes a cross-dataset reference against `index.json` (name to be settled — `realised_by` is the working label). Affects the doc's enables definition, the extractor, and RelatedPatterns rendering. Small once the carrier exists.

## Carried, not committed

From the vocabulary doc's own open questions and closure reflections — acknowledged here so they aren't re-lost, but not scoped in:

- `tensions-with` between qualities (doc question 5) — still waiting on two or three concrete examples.
- Negative-space marking for moves of last resort (state-disabled's all-`alternative` signature; T5 reflection) — node-level, rides only if B's constructs make it nearly free.
- `recommends` in the force-directed layout (doc question 2) and `related`-subcategory promotion to tags (doc question 3) — rendering and tagging questions, not vocabulary.
- Shared theoretical ancestry has no relation (T4 reflection): semantic-zoom and focus-and-context both descend from Furnas's degree-of-interest formalism, which sits *below* both patterns. The research takeaway was explicit — don't force a graph edge through a construct the language doesn't have — so the ancestry lives as parallel prose on both pages under an `alternative` edge. If a third DOI descendant lands (fisheye distortion, overview+detail as a move), revisit whether the ancestor wants a named home (foundation vs woven vocabulary, measured against Bridging Gulfs).
- `alternative` doing tangential work (T4 audit find): selection's `alternative: sorting` edge glosses an interaction ("sorting changes what a range selection refers to mid-flight"), not a mutual-alternative relation. One instance; fixed with workstream A's audit pass (2026-07-10, → `tangential`).
- Enacts-note audit (vocabulary doc 2026-06-23 follow-up, still unowned): pre-existing `enacts` labels were never checked against the Q-lens convention — some are "supports-Q"-shaped. The voicing advisory deliberately exempts `enacts` (no reverse reader), so nothing will mechanically resurface this; it needs its own pass, possibly a second advisory keyed on "supports/enables/provides Q" phrasings.
- Move-vs-artifact naming for the T6 kept twins (T6 reflection). Sections and Command menu keep the mechanism's name on the pattern side under the bilingual-same-name rule (pattern-role-model.md Decomposition signal 3), but they sit closer to artifact-names than Form does — the move behind Sections is adaptive disclosure. 
- Filtering's builder ↔ command menu, resolved to component level (T6 follow-up, 2026-07-09): the `enables: filtering` edge was dropped — the actor building a filter is making *filtering's* move; what recurs is the command-menu *mechanism*. The relation now lives as filtering.mdx's ComponentRef to the kept Storybook mechanism doc (`components-command-menu--docs`), the transient-feedback→toast idiom. A worked instance of the pattern→mechanism reference shape workstream D formalises.

## Not owned here

The decision-tree merge *implementation*, traversal UX, and situation-awareness accumulation (the graph-situation direction's interactive side); the index.json validator (closure plan workstream 2); any content-side tree-ification of action-consequences or status-feedback (those follow the schema, not the other way round).

## Sequencing

A is one sitting and can run immediately. A′ is a second independent sitting, executable any time. B is strawman → research gate → doc changelog entry, in that order, unhurried. C and D are verdicts that fall out of B and the validator respectively. Nothing here blocks the remaining closure territories (T4, T6).

## Progress log

- *2026-07-10 — Workstream A executed and closed.* Full record in the vocabulary doc's 2026-07-10 changelog entry. The four conversational `instantiates` are `enables`; the audit re-typed five more edges (user-opening→inquiry-user to `precedes`, learnability→agency to `related`, deletion→undo to `precedes`, sections→progressive-disclosure to `instantiates`, selection↔sorting to `tangential`), inverted help↔annotation, dropped the mediated progressive-disclosure→bot edge, resolved the transparent-reasoning→activity-log contradictory pair, and closed the last purist-stance quality leak (collaboration-foundation's `composed-of: privacy` → `enacts`). Both advisories (mixed cluster, notes voicing) live in the extractor's axis-check block; 18 notes reworded to subject-naming form. Voicing residue held deliberately: five relation glosses that read fine from both ends, plus two condition-bearing notes (good-defaults→autofill, live-presentation→generated-content) left for workstream B per item 5's gate — they are live specimens for the condition construct.
- *2026-07-10 — Traversal review of A's output.* Four situations walked through the changed territory; the re-typings held. Follow-ups applied: `group` now emitted as node metadata (conversation's 13 constituents were a flat list to graph consumers), voicing rule promoted into the doc's authoring model and `.claude/rules/pattern-content.md`, deletion→undo bundle added as a B sighting, precedes sweep added as workstream A′, enacts-note audit added to the carried list, and open question 5 now records that any quality↔quality treatment must decide support alongside tension (`learnability related agency` is the specimen).
- *2026-07-10 — Workstream A′ executed and closed.* Full record in the vocabulary doc's precedes-sweep changelog entry. All 93 `precedes` edges audited; 24 re-typed or dropped: 5 to `instantiates` (command-menu→searching, next-best-action→suggestion, wizard→progressive-disclosure, bot→assistance, bot→delegation), 11 to `enables` (incl. bounded-choice→filtering, state-empty→onboarding, hub-and-spoke→workspace, the data-entry assist trio, prompt/conversation/bot recomposition), 5 horizontal (wizard↔next-best-action `alternative`; autocomplete/autofill↔form and text-lense↔conversation `complements`; interaction↔searching `related`), 1 inverted in place (prompt precedes bot), 4 dropped as mediated (suggestion→bot, prompt→collaboration, navigation-overview→workspace, wizard→template). The assistance escalation ladder (good-defaults→autocomplete/autofill/ATC, autocomplete→ai-completion) held as `precedes` for workstream B — its "B takes over when…" shape is the conditional construct; the doc's unencoded Autocomplete-instantiates-Good-defaults example is logged there as a B decision. Extractor clean: precedes 93→69, voicing flags unchanged at the seven deliberate holds, same-altitude instantiates 4→7 (all three additions deliberate).
- *2026-07-10 — A′ follow-up trio.* From the sweep's reflection: `enables` definition softened from strict dependency to incorporation (aligning text with settled corpus practice), foundation tiebreaker written down (concept applied → `instantiates`, substrate produced → `precedes`), and a note-verb advisory added to the extractor (note phrasing vs stored type). Advisory retro-detects the sweep; after two false-positive note rewords (workspace, sections) and one genuine reword (searching→data-view's hosting-flavoured gloss), one finding stands deliberately: the good-defaults→assisted-task-completion ladder specimen held for B. The register question (designer traversal vs actor runtime) and the hosting evidence from A′ are recorded in workstreams B and C respectively; the register decision itself stays behind B's research gate.
- *2026-07-10 — Workstream B research gate run.* Findings in research/situation-constructs/2026-07-10.md; verdicts inline above (Research gate result). Retrieval was step-2b canon reads — the load-bearing literatures (pattern-form canon, DMN, adaptive hypermedia, situation calculus, statecharts) are venue-locked; arxiv/S2 contributed only the hyper-relational-KG datum. All five breakage questions answered; strawman survives amended (one authorable home for a judgement; no corpus-wide condition vocabulary; consumer contract defends the stance; designer register primary with divergence as the decompose signal). Hosting (workstream C) gained the spatial-inclusion datum from the doc's own Winston et al. canon.
- Remaining: B schema sitting (constructs + doc changelog entry, per the gate result above), then C and D verdicts.
