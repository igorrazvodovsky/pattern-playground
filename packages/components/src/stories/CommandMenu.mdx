import { Canvas, Story, Meta, Subtitle } from '@storybook/addon-docs/blocks';
import * as CommandMenuStories from './CommandMenu.stories.tsx';

<Meta of={ CommandMenuStories } tags={['activity-level:action', 'atomic:pattern', 'role:component', 'lifecycle:seeking', 'mediation:individual']} />

# Command menu

A combobox composition with hierarchical drill-in and AI fallback for invoking commands by name.

<Story of={ CommandMenuStories.Basic } />

It has two presentations: inline (above) and inside a modal dialog opened by a keyboard shortcut.

<Story of={ CommandMenuStories.Dialog } />

## Related components

- [Combobox](../?path=/docs/components-combobox--docs) — the mechanism this composition builds on
- [Keyboard key](../?path=/docs/components-keyboard-key--docs) — renders the shortcut hints
- [Context menu](../?path=/docs/components-context-menu--docs) — pointer-driven and target-aware, where the command menu is keyboard-driven and name-based
