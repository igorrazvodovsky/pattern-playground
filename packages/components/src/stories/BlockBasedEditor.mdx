import { Canvas, Story, Meta, Subtitle } from '@storybook/addon-docs/blocks';
import * as BlockBasedEditorStories from './BlockBasedEditor.stories.tsx';

<Meta of={BlockBasedEditorStories} tags={['activity-level:action', 'atomic:composition', 'role:component', 'lifecycle:sense-making', 'mediation:individual']} />

{/* > 🙂 **Fun meter: 5/5** Foundation for a block-based fun. */}

# Block-based editor

The composition of structured content from modular blocks, balancing the fluidity of prose with the precision of structured data.

<Story of={BlockBasedEditorStories.Basic} />

This is the reusable substrate (`@pkg/demos/block-based-editor`) shared between this story and the pattern site; the [bubble menu](../?path=/docs/components-bubble-menu--docs) is currently realised within it. Select text to raise the bubble menu, or type `@` to trigger a reference.

The move it realises — composing content from modular blocks, balancing the fluidity of prose with the precision of structured data — lives in the pattern language as <PatternRef slug="block-based-editor">Block-based editor</PatternRef>.

## Related components

- [Bubble menu](../?path=/docs/components-bubble-menu--docs) — the contextual text-selection actions surfaced inside the editor
- [Reference](../?path=/docs/components-reference--docs) — the @-mention primitive the editor embeds

## To-do

- Adding blocks
- Floating menu
- Slash commands
- Creating AI task
- References
- Contextual actions
- Co-editing & attribution: Parallel human/AI editing with attribution chips or overlays.
