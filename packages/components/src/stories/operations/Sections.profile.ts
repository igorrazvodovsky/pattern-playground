import type { GenerativeProfile } from '../../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    'content that divides into a small, stable set of named parts where which-mode-to-show is a property of context, not of the content',
  produces:
    'a single container whose disclosure affordance (tabs vs. accordion) shifts with context without restructuring the content',
  enacts: 'adaptability to context, continuity of structure across affordances',
};
