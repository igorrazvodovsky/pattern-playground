import { Meta, Story } from '@storybook/addon-docs/blocks';
import * as MorphingControlsStories from './MorphingControls.stories';

<Meta of={MorphingControlsStories} tags={['activity-level:operation', 'atomic:pattern', 'role:pattern', 'mediation:individual']} />

# Morphing controls

A control that transforms its appearance and available actions in response to a state change, replacing the previous set of options with those relevant to the new mode.

## When to use

Use morphing controls when the completion of that action makes the original trigger meaningless. The control communicates the current state while offering the most logical next step.

Avoid when users need to see the full range of possible actions to build a mental model, or when the transition between states is ambiguous and requires explanation.

## Examples

### Toggle

<Story of={MorphingControlsStories.FollowUnfollow} />

### Multi-state close

Close with confirmation is a common pattern, but instead of a separate dialog, the control itself morphs to offer the confirm action. This keeps the user in context and reduces friction.

<Story of={MorphingControlsStories.MultiState} />

## Related patterns

- [Switch](../?path=/docs/primitives-switch--docs) & [Checkbox](../?path=/docs/primitives-checkbox--docs) — use when on/off state is enough
- Morphing provides <PatternRef slug="status-feedback">status feedback</PatternRef>
- <PatternRef slug="unavailable-actions">Unavailable actions</PatternRef> removes controls entirely; morphing keeps them present but transformed
- [Disabled state](./?path=/docs/operations-disabled-state--docs) greys out controls without indicating the new state
- <PatternRef slug="progressive-disclosure">Progressive disclosure</PatternRef> — a broader pattern