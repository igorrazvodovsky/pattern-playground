import type { GenerativeProfile } from '../../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    'a piece of information the system cannot enumerate or infer — it has to be supplied',
  produces: 'a free-form text slot the actor can write into',
  enacts: 'openness to anything sayable; trust in what the actor types',
};
