import type { GenerativeProfile } from '../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    'a single choice the actor must make from a set of mutually exclusive options',
  produces: 'a visible selection state showing which option is currently chosen',
  enacts: 'legibility of mutual exclusivity, continuity with the surrounding flow',
};
