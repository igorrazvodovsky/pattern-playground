import type { GenerativeProfile } from '../../../pattern-profile';

export const profile: GenerativeProfile = {
  operatesOn:
    'a heterogeneous collection of subjects that need to be browsed at a glance and compared against each other',
  produces:
    'a uniformly bounded representation per subject — a same-shaped frame across the collection — that lets attributes, media, and actions read in one pass',
  enacts:
    'scannability of the collection, density-comparability across items, legibility of each subject as one thing',
};
