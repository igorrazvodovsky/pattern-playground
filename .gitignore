# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static
storybook-static-react
storybook-static.zip
.netlify
.env
.obsidian
plans/backlog-sources.md

# Astro
.astro/
apps/patterns/public/
.claude/worktrees/

# Storybook static build, copied into the site's actual publicDir (root
# public/, per apps/patterns/astro.config.mjs) so /storybook resolves on the
# deployed site. Refreshed by `npm run build:storybook-into-site`.
public/storybook/

# Cached Storybook index.json for the cross-reference validator's fallback
# (refreshed by `npm run build:storybook-index`, never deployed itself)
apps/patterns/storybook-index/

# One-time migration baseline (regenerated by hand, not tracked)
apps/patterns/src/data/pattern-graph.baseline.json