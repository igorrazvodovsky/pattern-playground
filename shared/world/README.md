The authoritative fixture world: bare individuals, the action log that
produced them, the relation and action-type registries, and declarative rules.
`facts.json` is generated — run `npx tsx scripts/world/replay.ts` after editing
the log, and never edit it by hand.

Specification: [docs/specs/world-ontology.md](../../docs/specs/world-ontology.md).
